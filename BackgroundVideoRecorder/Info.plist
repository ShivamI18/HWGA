<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- App Name -->
    <key>CFBundleName</key>
    <string>BackgroundVideoRecorder</string>

    <!-- App Version -->
    <key>CFBundleShortVersionString</key>
    <string>1.0</string>

    <key>CFBundleVersion</key>
    <string>1</string>

    <!-- Main storyboard reference (if using UIKit) -->
    <key>UILaunchStoryboardName</key>
    <string>LaunchScreen</string>

    <!-- Supported device orientations -->
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>

    <!-- Required device capabilities -->
    <key>UIRequiredDeviceCapabilities</key>
    <array>
        <string>arm64</string>
        <string>video-camera</string>
    </array>

    <!-- Camera usage permission (Required for video recording) -->
    <key>NSCameraUsageDescription</key>
    <string>This app requires access to the camera to record videos.</string>

    <!-- Microphone usage permission (Not needed since we are not using the mic) -->
    <key>NSMicrophoneUsageDescription</key>
    <string>This app does not use the microphone.</string>

    <!-- Background Modes: Allows recording in the background -->
    <key>UIBackgroundModes</key>
    <array>
        <string>audio</string>
    </array>

    <!-- Prevent iOS from suspending the app when recording -->
    <key>UIApplicationExitsOnSuspend</key>
    <false/>

</dict>
</plist>
